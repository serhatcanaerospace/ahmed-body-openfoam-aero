# Usage — Ahmed Body Mesh Sensitivity

This planned case will use `simpleFoam`.

Expected workflow:

```bash
cd cases/mesh_sensitivity
# generate mesh
# run solver
# post-process validation metrics
```

Generated mesh and solver outputs should stay out of Git.
